# Core assessment + companion notebook (CPU, no GPU needed)
numpy>=1.24
pandas>=2.0
scipy>=1.10
scikit-learn>=1.3
networkx>=3.0
matplotlib>=3.7
jupyter>=1.0

# Verified working with:
#   numpy 2.5.0  pandas 3.0.3  scipy 1.18.0  scikit-learn 1.9.0
#   networkx 3.6.1  matplotlib 3.11.0  (Python 3.13)

# analysis/run_dscript_predict.py additionally needs (GPU, not required to
# reproduce the committed results):
#   torch  h5py  tqdm  dscript==0.2.8
